#!/bin/bash
if [[ -f /tmp/.pai-privacy-mode ]]; then
    echo '{"text": "🧅 Tor", "class": "active", "tooltip": "Privacy Mode: ON — traffic routed through Tor"}'
else
    echo '{"text": "", "class": "inactive"}'
fi
