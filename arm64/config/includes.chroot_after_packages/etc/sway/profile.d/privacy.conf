# PAI Profile: Privacy Mode
# Keybindings: Tor Browser only (no clearnet browser shortcut)
# (terminal $mod+Return comes from main config)

bindsym $mod+b exec torbrowser-launcher
bindsym $mod+Shift+t exec foot -e htop
