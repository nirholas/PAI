# PAI Profile: Developer
# Keybindings: terminal, browser, file manager, text editor

bindsym $mod+Return exec foot
bindsym $mod+b exec firefox http://localhost:8080
bindsym $mod+e exec thunar
bindsym $mod+p exec drawing
